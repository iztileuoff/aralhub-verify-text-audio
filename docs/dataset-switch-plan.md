# План: переключение на новый датасет

> Обновлено: 2026-08-14. **Прод на `ae7a7cd`. Переключение выполнено:**
> `DATASET_MAIN_FILE_ID=10`, активный датасет — **file 10**, метка `v3`, **4 142 текста**
> (партии `207.xlsx` + `2500-2700.xlsx` + `2700-3000.xlsx` + `208-1022.xlsx` +
> `3201-3300.xlsx` + `1022-1501.xlsx` + `3301-4000.xlsx` + `1502-2001.xlsx` +
> `4001-4400.xlsx` + `2002-2301Durisi.xlsx` + `4401-4600.xlsx`). Прежний file 8 помечен `v2`.
>
> **Заказчик прислал не всё.** Покрытие `transcript_id` на 2026-08-14: `1…2300`,
> `2499…2999`, `3200…4599`. Не приходили **`2301…2498`** (198 текстов) и **`3000…3199`**
> (200 текстов). Верхней границы у общего файла, судя по всему, нет — партии идут дальше
> 4000, так что «сколько всего текстов будет» заказчик не объявлял.
>
> Фазы 1–4 закрыты (#36–#42), медленные админские списки — #44. Датасет пришёл не `.txt`,
> а **партиями `.xlsx`**, поэтому загрузка пошла через `import:excel-file`, а не
> `import:text-file`.
>
> **Следующие партии** — тем же датасетом, без нового переключения:
> `php artisan import:excel-file <файл>.xlsx --file-id=10 --label=v3`.
> Перед заливкой убедиться, что в файле **есть строка заголовков** (см. Фазу 2):
> из одиннадцати партий её не было у **десяти**. У восьмой вдобавок появилась пустая
> **десятая колонка** — её надо отбрасывать, иначе шапка из девяти имён разъедется с
> данными.
>
> ⚠️ **Имя файла ничего не гарантирует — проверять содержимое до заливки.** У всех партий
> оно врёт на единицу, а `2002-2301.xlsx` (2026-08-13) оказался вообще не той партией:
> внутри `kaa-001022`…`kaa-001500`, строка в строку то же, что уже залитый
> `1022-1501.xlsx`. Заказчик прислал замену — `2002-2301Durisi.xlsx`, десятая партия, — и
> она уже настоящая; блок закрыт наполовину, осталось `2301…2498`.
>
> ⚠️ **Неозвученные тексты кончаются быстрее, чем приходят партии.** Хроника 2026-08-13:
> перед пятой партией свободных «ни разу не озвученных» текстов оставалось **2**, после
> неё — 101; к шестой (13:53) от них не осталось почти ничего, 477 новых подняли запас до
> 476; к седьмой (16:45) свободных снова было **2**, и 690 новых текстов — весь нынешний
> запас. За день крауд закрыл около 2 000 записей и довёл до трёх аудио 1 958 текстов.
> Партия в 100 строк расходуется за часы, в 500–700 — примерно за день. К утру 2026-08-14
> (12:57) от 694 свободных осталось **65**, и 199 новых подняли запас до 177.
>
> Открыто: выгрузка хвоста file 8 (#35, без срока) и освобождение 223 удерживаемых
> текстов file 8 (Фаза 3, косметика).
>
> Тем же днём и тем же релизом прод переехал на **Cloudflare R2** (новые записи →
> `audio.iztileuoff.uz`, прежние 129 072 читаются с `yandex-s3` по `storage_disk`).
> К датасету это отношения не имеет, но объясняет, почему в релизе была миграция.

## Задача

Загрузить в прод новый датасет текстов. Требования заказчика:

1. **Все предыдущие данные должны выпасть из статистики** — дашборд, отчёты спикеров и
   модераторов, профили пользователей считают только активный датасет.
2. **Новый датасет должен явно отличаться от предыдущих** — по `file_id`, имени файла и
   группе экспорта.

Принятые решения (2026-08-12):

| Вопрос | Решение |
|---|---|
| Как привязать статистику | **Только активный датасет** — всё считается от `config('dataset.main_file_id')`. Параметра выбора датасета в отчётах не будет. |
| 39 087 неозвученных текстов `file_id = 8` | **Отбрасываются.** После переключения в очередь спикеров не попадают. |
| Итоговые счётчики спикеров (профиль, рейтинг) | **Обнуляются** — тоже считаются по активному датасету. |
| 368 неотмодерированных аудио `file_id = 8` | **Отбрасываются.** После переключения исчезнут из очереди модерации и останутся записанными, но непроверенными — а значит, невыгружаемыми навсегда. Принято сознательно. |
| Невыгруженный хвост file 8 (29 677 принятых аудио) | **Без срока.** Выгружается явным `--file-id=8` когда угодно, в том числе после переключения (#35). |

Старые данные остаются в базе, просто перестают отображаться.

**Все решения из таблицы исполнены 2026-08-12 вечером.** Решение по 368 аудио
подтверждено повторно уже после того, как модерация возобновилась: 2026-08-12 в 12:36
модератор проверил одну запись из этого остатка, заказчик всё равно подтвердил, что
дочищать очередь не нужно. Эти 368 больше не видны в очереди и никогда не попадут
в выгрузку — `audio:export-correct` берёт только `is_correct = true`.

---

## 1. Состояние прода до переключения (2026-08-12)

| file_id | Файл | Тексты (`is_main`) | Аудио | Без модерации | Выгружено |
|---|---|---|---|---|---|
| 1–3 | `train.tsv`, `test.tsv`, `dev.tsv` | 1 990 | 16 458 | 0 | 16 256 |
| 4–7 | `FINISH_DATASET*.xlsx`, `final qq.xlsx` | 2 098 | 16 763 | 0 | 16 550 |
| **8** | **`karakalpak_corpus_v2_all_texts.txt`** | **135 767** | **95 851** | **368** | **45 260** |

Детали по активному `file_id = 8`:

- 135 667 строк импорта + 100 приветственных текстов (`texts:add-greetings`), плюс 666
  `is_split_part` записей (не датасетные тексты).
- **39 087** текстов ни разу не озвучены, **3 469** помечены `has_text_error`,
  **223** удерживаются спикерами (`speak_started_at` не сброшен).
- Аудио: 95 185 озвучено, 74 937 `is_correct = true`, **368** ждут модерации.

Всего по базе: **129 072** аудио, из них **50 340** без `edit_converted_audio_duration`
и **51 006** ещё не выгружены.

Таблица `exports`: `Legacy (file_id < 8)` — 32 806, `file_id = 8` — 45 260.

## 1а. Состояние после переключения

| file_id | Файл | Тексты | Роль |
|---|---|---|---|
| 1–7 | legacy | 4 088 | архив |
| 8 | `karakalpak_corpus_v2_all_texts.txt` (`v2`) | 135 767 | бывший активный, скрыт |
| **10** | **`207.xlsx` (`v3`)** | **4 142** | **активный** |

Строка file 10 — на 2026-08-14 после одиннадцатой партии; в момент переключения было 204.
Имя файла в записи `File` остаётся от первой партии (`207.xlsx`) — дописывание не меняет
`path`, различать датасеты нужно по метке.

В `.env` прода добавлено `DATASET_MAIN_FILE_ID=10`, конфиг пересобран.

**Датасет получил `file_id = 10`, а не 9, как планировалось.** Id 9 израсходован в тот же
день временной записью `File`, созданной при проверке R2 и сразу удалённой: MySQL не
переиспользует счётчик `AUTO_INCREMENT` после удаления строки. Функционально разницы нет,
но во всех командах и переменных нужен именно **10**. Проверять счётчик через
`information_schema.TABLES.AUTO_INCREMENT` бесполезно — значение там приблизительное,
перед вставкой оно показывало 9.

⚠️ Правка `.env` на проде: у файла **не было завершающего перевода строки**, поэтому
`echo 'X=1' >> .env` приклеил новую переменную к последней строке и сломал `AUDIO_DISK`.
Приложение работало на закэшированном конфиге и пользователи этого не заметили, но
`config:cache` в таком состоянии уронил бы загрузку аудио. Дописывать только через
`printf '\nX=1\n' >> .env`, потом обязательно `grep -c '^X=' .env`.

---

## 2. Откуда код берёт `file_id` — три разных механизма

### A. Через `config('dataset.main_file_id')` — переключается переменной окружения

`Text::scopeMainFile()`, `SpeakTextController:41`, `LongAudioController:19`,
`LongAudioProcessedController:26`, `LongAudioPartTextUpdateController:54`,
часть `DailyQuotaController`.

### B. Захардкоженная константа `FILE_ID = 8` — не переключится

**Исправлено в #36** — константы больше нет ни в одном файле `app/`:
`ReportSpeakerController`, `ReportModeratorController`, `ExportReportUserController`,
`ExportReportCommand`, `ExportDailyReportCommand` берут значение из конфига.

Было: после переключения все отчёты по спикерам и модераторам продолжили бы
показывать данные старого датасета.

### C. Фильтра по файлу нет вообще — старое и новое смешается

**Исправлено в #37 и #38.** Что было не отфильтровано:

| Файл | Что смешивалось | Issue |
|---|---|---|
| `DailyQuotaController` | восемь счётчиков текстов и оба сегодняшних счётчика аудио | #37 |
| `ProfileReportController` | личный отчёт пользователя (спикер + модератор) целиком | #37 |
| `Verify/UserController` | все `withCount`: итоговые и дневные счётчики по пользователям | #37 |
| `SpeakTextController` | запрос удерживаемого текста | #38 |
| `ModeratorAudioController`, `ModeratorTextController` | очередь модерации | #38 |
| `FinishedAudioController` | список готовых аудио | #38 |

Повторяющийся `whereHas('text', …->mainFile())` свёрнут в `Audio::scopeMainFile()`.

**Исправлено в #44** — три админских списка, до которых Фаза 1 не дошла, и по данным Pulse
именно они остались медленными (`finished/audio/texts` — 15 с, `finished/edit/texts` — 9 с,
`finished/check/texts` — 1.6 с): они обходили все 143 361 текста и 129 408 аудио, потому что
фильтра по датасету у них не было вовсе.

| Файл | Что смешивалось |
|---|---|
| `FinishedAudioTextController` | список озвученных текстов + `leftJoinSub` по **всей** таблице аудио |
| `FinishedEditTextController` | список отредактированных текстов |
| `FinishedCheckTextController` | список проверенных аудио |

У всех трёх появился тот же выключатель, что у команд экспорта: **без параметра — активный
датасет**, `?file_id=8` — прежний. `Text::scopeMainFile()` и `Audio::scopeMainFile()` теперь
принимают необязательный id файла; параметр читается общим трейтом `ScopesDatasetFile`.
Побочный эффект, ожидаемый: списки перестали показывать разрезанные части (`is_split_part`)
— они не `is_main`, ровно как в счётчиках #37.

### D. Команды экспорта

**Исправлено в #39** — `{--file-id=}` без значения означает
`config('dataset.main_file_id')`; явный `--file-id=8` по-прежнему достаёт прежний датасет.

---

## 3. Риски перехода

1. ~~**`SpeakTextController`** — запрос удерживаемого текста без фильтра по файлу.~~
   Закрыто в #38. 223 удерживаемых текста всё равно нужно освободить в Фазе 3:
   фильтр не даст выдать их снова, но `speak_started_at` на них так и останется.
2. **Бэклог file 8 не выгрузится сам.** С #39 дефолт `--file-id` — активный датасет,
   поэтому 29 677 принятых аудио надо выгружать явным `--file-id=8` (#35, без срока).
3. **Кэш дашборда** — `DailyQuotaController`, `Cache::flexible` на 1 ч свежести / 2 ч
   устаревания. Ключ `daily_quota_data` чистит `optimize:clear`, а не `config:cache`:
   при переключении 2026-08-12 сначала выполнили только `config:cache`, и дашборд ещё
   какое-то время показывал бы счётчики старого датасета. Исправлено отдельным прогоном
   `optimize:clear && optimize`. **После смены `DATASET_MAIN_FILE_ID` всегда нужен
   `optimize:clear`, одного `config:cache` мало.**
4. ~~**`import:text-file`** пишет строки по одной внутри одной транзакции.~~
   Закрыто в #40: оба импорта (`import:text-file`, `import:excel-file`) пишут чанками,
   `--chunk=1000` по умолчанию.
5. **Дубликаты** — если в новом датасете встречаются предложения из старого, спикеры
   будут переозвучивать уже записанное. Для `.txt` — `import:check-duplicates`,
   для `.xlsx` отсев встроен в `import:excel-file` (внутри партии и против того, что
   уже лежит в этом же датасете). Против **других** датасетов ни то ни другое не
   проверяет — при добавлении партий это осознанный компромисс.
6. После переключения `edit_finished_texts_count` сразу станет равен размеру датасета:
   импорт проставляет `edit_finished_at` при загрузке. Это ожидаемо.

---

## 4. Фазы

### Фаза 0 — закрыть старый датасет ⛔ снята с критического пути

Решение 2026-08-12: **переключение не ждёт закрытия file 8.**

- ⛔ ~~Домодерировать 368 аудио~~ — отброшены сознательно (см. §Задача).
- ⏳ Выгрузка невыгруженного хвоста (29 677 принятых аудио, из них 29 011 без длительности) —
  **без срока**, #35. `audio:export-filenames --file-id=8` → Python → `update:duration` →
  `audio:export-correct --file-id=8 --name="v2 final"`.

Почему выгрузку можно отложить, а модерацию — нет: явный `--file-id=8` достаёт прежний
датасет независимо от конфига (#39), а очередь модерации выдаёт только активный (#38).
После переключения эти команды **нельзя запускать без `--file-id`** — уйдут в файл 10.

### Фаза 1 — код: привязать всю статистику к одному источнику ✅

Готова и **выкачена в прод 2026-08-12** (`caf04e3`, `PHP_BIN=/opt/php8.4/bin/php ./.deploy.sh`).
Миграций в релизе не было. Конфиг остался `8`, поэтому выдача текстов и очереди работают
как прежде, но счётчики уже считают только активный датасет:

| Показатель | Было | Стало |
|---|---|---|
| Тексты в счётчиках правок и дневной квоты | 142 290 | 135 767 |
| База сегодняшних счётчиков аудио | 129 072 | 95 185 |

- [x] Заменить 5 констант `FILE_ID = 8` на `config('dataset.main_file_id')` (#36)
- [x] `DailyQuotaController` — фильтр поднят на весь запрос текстов, `audioTodayStats` тоже (#37)
- [x] `ProfileReportController` — оба отчёта (#37)
- [x] `Verify/UserController` — все `withCount`-замыкания; итоговые счётчики
      пользователей обнулятся — так и задумано (#37)
- [x] `SpeakTextController` — фильтр в запросе удерживаемого текста (#38)
- [x] `ModeratorAudioController`, `ModeratorTextController`, `FinishedAudioController` (#38)
- [x] `ExportAudioFilenamesCommand`, `ExportCorrectAudioCommand` — `{--file-id=}` (#39)
- [x] Тесты: 139 passed (было 125). Появились первые тесты на отчёты спикеров/модераторов,
      `ModeratorAudioController` и `FinishedAudioController`

Что всплыло попутно:

- `report:daily` накапливал `$days`, `report:export` не сбрасывал `$registeredFrom` при
  повторном вызове в одном процессе — исправлено в #36.
- Счётчики модератора удваивались на разрезанных аудио: части копируют
  `moderator_finished_at` родителя, но их тексты не `is_main` — ушло само с фильтром (#37).

### Фаза 2 — загрузка датасета ✅

Заказчик прислал не `.txt` целиком, а **первую партию `.xlsx` на 206 строк** (остальное
дочищается), поэтому путь оказался другим, чем планировалось:

| Колонки в файле заказчика | `id`, `role`, `intent`, `text`, `script`, `style`, `contains`, `words`, `needs_native_review` |
|---|---|
| Строк в файле | 206 (`role`: client 156, agent 50) |
| Импортировано | **204** |
| Пропущено | 2 строки с пустым `text` (`kaa-000003`, `kaa-000072`) |

- [x] Залить файл в `storage/app/public/` на проде
- [x] `php artisan import:excel-file 207.xlsx --user=1` → создан **File #10**
- [x] Проверить: `rows_imported = 204`, `is_main = 1` у всех, пустых текстов нет,
      `transcript_id` заполнен у всех
- [x] Перевести импорт на чанковый `insert()` — сделано, `--chunk=1000` по умолчанию

Под партии переписан `import:excel-file` (см. коммит `bb96e78`): появился `--file-id`
для дописывания в существующий датасет, колонка читается без учёта регистра, `id` вида
`kaa-000042` превращается в `transcript_id = 42`, пустые строки и повторы отсеиваются
на месте — отдельный прогон `import:check-duplicates` для `.xlsx` не нужен.

**Следующие партии — только с `--file-id=10`**, иначе каждая станет отдельным датасетом
и потребует нового переключения.

**Партия 2 залита 2026-08-12** (`2500-2700.xlsx`, 201 строка, `kaa-002499`…`kaa-002699`,
все `role: client`): `import:excel-file 2500-2700.xlsx --file-id=10 --label=v3`. В file 10
стало **405** текстов.

⚠️ **Партия пришла без строки заголовков** — её вырезали из общего файла, и шапка не
повторилась. FastExcel принимает за шапку первую строку данных, поэтому колонки `text`
не находилось: импорт молча прошёл бы как «201 строка пропущена — пустой текст», а строка
`kaa-002499` пропала бы совсем. Шапку восстановили из значений съеденной строки, файл
пересобрали и залили 201 строку целиком. Теперь такой файл `import:excel-file`
**отклоняет** с подсказкой про заголовки. Проверять любую новую партию до заливки:
у первой строки должны быть имена колонок, а не `kaa-00xxxx`.

**Партия 3 залита 2026-08-12** (`2700-3000.xlsx`, `kaa-002700`…`kaa-002999`, `client` 251,
`agent` 49): `import:excel-file 2700-3000.xlsx --file-id=10 --label=v3` — 300 из 300, ни
пустых, ни повторов. В file 10 стало **705** текстов, в очереди спикеров — 689.

Шапки не было **и у неё** — то есть это не случайность, а то, как заказчик режет общий
файл. Порядок действий тот же: восстановить шапку
(`id, role, intent, text, script, style, contains, words, needs_native_review`) из
значений съеденной строки, пересобрать файл и только потом заливать. Гейт из #42 сработал
как задумано — импорт отклонил бы файл, а не потерял бы 300 строк.

**Партия 4 залита 2026-08-12** (`208-1022.xlsx`): `import:excel-file 208-1022.xlsx
--file-id=10 --label=v3` — 778 из 815, ни пустых, ни повторов внутри партии, **37 отсеяно
как уже загруженное**. В file 10 стало **1 483** текста, очередь спикеров — 1 455.

Имя файла врёт: внутри `kaa-000207`…`kaa-001021` (815 строк без пропусков), а не 208–1022.
Шапки снова не было — третий такой файл из четырёх.

⚠️ **Партии заказчика пересекаются по содержимому.** У всех 37 отсеянных текстов
парный id отличается ровно на **+2000**: `kaa-000667 ↔ kaa-002667`,
`kaa-000683 ↔ kaa-002683`, `kaa-000924 ↔ kaa-002924` и так далее. То есть блок
`2499–2999` частично повторяет блок `499–1021` — это дефект генерации у заказчика, а не
ошибка заливки. Сейчас его гасит отсев `import:excel-file` (риск №5 в §3), но он видит
только **этот же** датасет: пересечение со старым file 8 не проверяется никем.

**Партия 5 залита 2026-08-13** (`3201-3300.xlsx`): `import:excel-file 3201-3300.xlsx
--file-id=10 --label=v3` — **99 из 100**, ни пустых, ни повторов внутри партии, 1 отсеян
как уже загруженный. В file 10 стало **1 582** текста.

Внутри `kaa-003200`…`kaa-003299` (100 строк без пропусков) — имя файла снова врёт, на
единицу. `role`: client 99, agent 1. Шапки не было — четвёртый такой файл из пяти.

Пересечение партий подтвердилось и здесь, но с другим шагом: отсеян `kaa-003237`
(«Awa meni tawıpsız»), а его пара — `kaa-000237`, разница ровно **−3000**. В партии 4 шаг
был **+2000**. То есть повторяется не один конкретный блок: заказчик режет общий файл так,
что куски накладываются друг на друга в разных местах.

**Партия 6 залита 2026-08-13** (`1022-1501.xlsx`): `import:excel-file 1022-1501.xlsx
--file-id=10 --label=v3` — **477 из 479**, ни пустых, ни повторов внутри партии, 2 отсеяно
как уже загруженное. В file 10 стало **2 059** текстов, ни разу не озвученных — 480.

Внутри `kaa-001022`…`kaa-001500` (479 строк без пропусков) — имя врёт на единицу в конце.
`role`: client 300, agent 179 — агентских реплик заметно больше, чем в прошлых партиях.
Шапки не было — пятый такой файл из шести.

Шаги пересечения снова разные: `kaa-001200` совпал с `kaa-003200` (**+2000**), а
`kaa-001497` — с `kaa-000497` (**−1000**). Вместе с −3000 из партии 5 это уже три разных
смещения, то есть искать «тот самый повторяющийся блок» бессмысленно — куски общего файла
у заказчика накладываются произвольно. Отсев `import:excel-file` держит это молча; при
переходе на новый `file_id` защита исчезнет (риск №5 в §3).

**Партия 7 залита 2026-08-13** (`3301-4000.xlsx`): `import:excel-file 3301-4000.xlsx
--file-id=10 --label=v3` — **690 из 701**, ни пустых, ни повторов внутри партии, 11 отсеяно
как уже загруженное. В file 10 стало **2 749** текстов, ни разу не озвученных — 694.

Внутри `kaa-003300`…`kaa-004000` (701 строка без пропусков), имя врёт на единицу в начале.
`role`: client 651, agent 50. Шапки не было — шестой такой файл из семи.

Пересечения впервые собрались в кучу: у **10 из 11** отсеянных шаг **−3000**
(`kaa-003317` ↔ `kaa-000317` и далее), одиннадцатый — `kaa-003695` ↔ `kaa-002695`
(**−1000**). Всего по трём последним партиям набрано четыре разных смещения
(+2000, −1000, −3000 и снова −3000): блок `3300–4000` заметно перекрывает `300–1000`.

**Партия 8 залита 2026-08-13** (`1502-2001.xlsx`): `import:excel-file 1502-2001.xlsx
--file-id=10 --label=v3` — **500 из 500**, первая партия вообще без отсева. В file 10 стало
**3 249** текстов, ни разу не озвученных — 505.

Внутри `kaa-001501`…`kaa-002000` (500 строк без пропусков) — имя врёт на единицу с обоих
концов. `role`: client 450, agent 50. Шапки не было — седьмой такой файл из восьми.

⚠️ **Новая форма дефекта: лишняя десятая колонка**, пустая во всех 500 строках. Шапка из
девяти имён на таком файле разъезжается с данными, поэтому перед сборкой хвостовые колонки
проверяются на пустоту и отбрасываются. Проверять это у каждой следующей партии.

**Карта покрытия `transcript_id` после восьмой партии:** `1…2000`, `2499…2999`,
`3200…4000`. Не присланы **`2001…2498`** и **`3000…3199`**. Мелкие дыры внутри блоков
(3, 72, 667, 683, 688, 695, 698, 924, 951, 953–981, 1200, 1497, 3237, 3317, 3645…) — не
потери: это пустые тексты первой партии и отсеянные дубли, чьи пары лежат в датасете под
другим id.

**Партия 9 залита 2026-08-13** (`4001-4400.xlsx`): `import:excel-file 4001-4400.xlsx
--file-id=10 --label=v3` — **397 из 400**, ни пустых, ни повторов внутри партии. В file 10
стало **3 646** текстов, ни разу не озвученных — 402.

Внутри `kaa-004000`…`kaa-004399` (400 строк без пропусков), имя врёт на единицу в начале.
`role`: **agent 201, client 199** — впервые агентских реплик больше половины; во всех
прошлых партиях преобладал должник. Шапки не было — восьмой такой файл из девяти.

Отсеяно 3: `kaa-004000` — стык с седьмой партией (та кончалась ровно на нём), плюс
`kaa-004093` и `kaa-004098` с уже привычным шагом **−3000**. Покрытие стало `1…2000`,
`2499…2999`, `3200…4399`.

⚠️ **Вместе с этой партией пришёл `2002-2301.xlsx`, и это не та партия.** Внутри
`kaa-001022`…`kaa-001500` — строка в строку `1022-1501.xlsx`, залитый шестой партией
(файлы различаются побайтно, содержимое идентично). Не импортировался: `import:excel-file`
отсеял бы все 479 строк, но `rows_total` у записи `File` вырос бы на 479 впустую. Заказчик
пришлёт файл заново. **Проверять содержимое каждой партии до заливки — имя файла не
доказательство**, тем более что отсев дублей молча съел бы такую ошибку без единого следа.

**Партия 10 залита 2026-08-13** (`2002-2301Durisi.xlsx`) — замена той самой не той партии,
и на этот раз внутри действительно `kaa-002001`…`kaa-002300` (300 строк без пропусков, имя
врёт на единицу с обоих концов). `import:excel-file 2002-2301Durisi.xlsx --file-id=10
--label=v3` — **297 из 300**, пустых нет, с датасетом пересечений **ноль**. В file 10 стало
**3 943** текста; свободных ни разу не озвученных — 694 (всего без аудио 761, из них 5
удерживаются спикерами, остальные — split-part и прочие исключения очереди).

Шапки не было — девятый такой файл из десяти; восстановлена по обычной схеме, лишней
десятой колонки нет.

⚠️ **Первая партия с повторами внутри себя.** Отсеяны 3 — `kaa-002282`, `kaa-002283`,
`kaa-002284`, и это подряд идущая тройка, дословно повторяющая `kaa-002275`…`kaa-002277`
(«Tıńlań men emespen…», «Yaq qáte, maǵan tiyisli emes», «Awa biraq, basqa jerge qońıraw
etiń»). Раньше все пересечения приходили извне партии; смещение здесь **−7** и внутреннее.
Покрытие стало `1…2300`, `2499…2999`, `3200…4399`, дыры внутри — только эти три id.

`role`: **agent 200, client 100** — ровно две трети агентских, перекос ещё сильнее, чем в
девятой партии (там впервые стало 201/199). Из отсеянных все три — `client`, так что в
датасет легло 200 агентских и 97 клиентских.

**Партия 11 залита 2026-08-14** (`4401-4600.xlsx`): `import:excel-file 4401-4600.xlsx
--file-id=10 --label=v3` — **199 из 200**, пустых нет, повторов внутри партии нет, 1 отсеян
как уже загруженный. В file 10 стало **4 142** текста; свободных ни разу не озвученных —
177 (всего без аудио 246, из них 65 с `has_text_error` и 4 удерживаются спикерами).

Внутри `kaa-004400`…`kaa-004599` (200 строк без пропусков) — имя врёт на единицу с обоих
концов. Шапки не было — десятый такой файл из одиннадцати; лишней десятой колонки нет.

`role`: **client 200** — партия целиком клиентская, второй такой случай после второй
(`2500-2700.xlsx`). После агентского перекоса девятой и десятой партий (201/199 и 200/100)
заказчик снова вернулся к репликам должника; предсказать состав по номеру блока нельзя.

Отсеян 1 — `kaa-004412` («Bólip tólesem bolama ótinish jazıwım kerek pe»), его пара
`kaa-000464` (`text #143920`). Смещение **−3948**, пятое разное по счёту: до этого были
+2000, −1000, −3000 и внутреннее −7. Покрытие стало `1…2300`, `2499…2999`, `3200…4599`.

⚠️ **У одного текста оказалось 4 аудио при лимите 3** (`text #144814`, `transcript_id
1297`, не split-part): три записи легли в 14:32, 14:58 и 16:32, четвёртая — в 16:33,
через 41 секунду после третьей, и у неё пустой `speak_started_at`. Похоже на гонку между
выдачей текста и инкрементом `audio_count`, но не разобрано; один случай на 2 749 текстов.
На экспорт не влияет — лишняя запись просто добавляет ещё один голос.

⚠️ **Первый проход по датасету почти догнал заливку.** Перед пятой партией во всём file 10
оставалось **2** ни разу не озвученных текста (плюс 2 удерживаемых спикерами) — за сутки
крауд озвучил все 1 483. Распределение на момент заливки: `audio_count` = 3 у 576 текстов,
= 2 у 856, = 1 у 25. Дальше очередь живёт вторым и третьим проходами (лимит 3 аудио на
текст, `dataset.max_audio_per_text`) — это ещё 948 текстов, но новых голосов на текст
партия в 100 строк добавляет примерно на день.

Через полтора часа, к шестой партии, распределение стало 3 у **1 100**, 2 у 422, 1 у 28 —
то есть за это время закрыто около 600 записей. Ещё через три часа, к седьмой партии:
3 у **1 958**, 2 у 26, 1 у 33. Ещё через час, к восьмой: 3 у 1 958, 2 у 119, 1 у **619** —
то есть 690 текстов седьмой партии получили по первой записи меньше чем за час, и крауд
пошёл по ним вторым проходом. К девятой (19:15): 3 у 1 958, 2 у 493, 1 у 730 — за полтора
часа ещё около 500 вторых записей, но **третий проход стоит на месте**: 1 958 текстов с
тремя аудио не растёт с 16:45. Похоже, спикеры упираются в правило «один спикер — одно
аудио на текст» (#43): третий голос находится не для всякого текста. Темп стоит держать
в уме, планируя следующие партии: 100 строк расходуются быстрее, чем приходит замена.

К десятой партии (19:56) распределение **не сдвинулось ни на одну запись**: 3 у 1 958,
2 у 493, 1 у 730 — те же числа, что и в 19:15. За 41 минуту крауд не закрыл ни одного
аудио; из свободной очереди ушло 5 текстов (их держат спикеры). Это вечер, так что скорее
конец рабочего дня, чем упор в правило #43, — но проверять стоит по обоим признакам:
«стоит только тройка» и «стоит всё».

К одиннадцатой партии (2026-08-14, 12:57): 3 у **1 958**, 2 у 1 715, 1 у 204, 0 у 65. За
ночь и утро закрыто примерно 1 200 вторых записей, а число текстов с тремя аудио **не
изменилось с 16:45 предыдущего дня** — двадцать часов подряд.

⚠️ **Объяснение «третий проход стоит» через #43 оказалось неверным — дело в порядке
очереди.** `SpeakTextController` сперва раздаёт тексты с `audio_count IS NULL`, а когда
таких нет — берёт **минимальный** `audio_count` среди оставшихся и выдаёт только этот
уровень. Пока в датасете есть хоть один текст с 0 или 1 аудио, тексты с двумя записями
спикеру просто не показываются, и третий голос не может появиться в принципе. На 12:57
таких оставалось 65 и 204 — то есть до уровня «2» очередь ещё не дошла. Правило «один
спикер — одно аудио на текст» (#43) сюда добавляется вторым слоем, но само по себе
заморозку не объясняет. Практический вывод: **третий проход закрывается только после того,
как все тексты получат по две записи**, а каждая новая партия отбрасывает очередь обратно
на уровень 0 — при нынешнем темпе заливки лимит 3 недостижим.

После дописывания партии счётчики дашборда живут на кэше `daily_quota_data`
(`Cache::flexible`, 1 ч свежести): либо ждать, либо снять ключ вручную.

⚠️ Все 206 строк помечены `needs_native_review = 1` — вычитка ещё идёт. Если текст
поправят после того, как его озвучили, запись под него станет негодной: аудио привязано
к конкретному тексту.

### Фаза 3 — переключение ✅

Выполнено 2026-08-12 вечером.

- [x] В прод `.env`: `DATASET_MAIN_FILE_ID=10`
- [x] `php artisan config:cache` — пересобрать кэш конфига
- [ ] Освободить 223 удерживаемых текста file 8: `speak_started_at` и `edit_speaker_id` → `null`
      (фильтр из #38 уже не даст их выдать, но флаг на строках останется)
- [x] Проверка после переключения:

| Показатель | Значение |
|---|---|
| `config('dataset.main_file_id')` | 10 |
| Текстов в очереди спикеров | 204 |
| Аудио в очереди модерации | 0 (368 из file 8 выпали — так и задумано) |
| `api.verify.aralhub.uz/up` | 200 |

### Фаза 4 — визуальное разделение датасетов ✅ код готов

- [x] Колонка `label` в `files` — человекочитаемое имя датасета (`v3`)
- [x] `is_active` — **не колонка, а аксессор** `id === config('dataset.main_file_id')`
- [x] Отображение активного датасета в админке: `GET admin/dataset` + поля `label`/`is_active`
      в `GET admin/files`; переименование — `PATCH admin/files/{file}`
- [x] `--label=` у `import:excel-file` (на дописывании переименовывает) и `import:text-file`
- [x] Конвенция имён групп экспорта: без `--name` имя строится само —
      `«{label} batch YYYY-MM»`, без метки — `«file {id} batch YYYY-MM»`
- [x] Тесты: 188 (было 139 после Фазы 1; локально 2 падения WebLoginTest — нет Vite-манифеста)
- [x] Выкачено на прод 2026-08-12 (`5e84211`), метки проставлены: file 8 → `v2`, file 10 → `v3`

**Почему `is_active` не колонка.** Хранимый флаг — второй источник правды: разойдись он
с `DATASET_MAIN_FILE_ID`, админка показывала бы один датасет, а очереди, счётчики и отчёты
считались бы по другому — ровно тот класс ошибки, из-за которого затевалась Фаза 1.
Аксессор физически не может разойтись. Цена решения: переключение датасета по-прежнему
только через `.env` + `optimize:clear`, кнопки «сделать активным» в админке не будет.

После деплоя (метки нужны, иначе группы экспорта поедут как `file 8 batch …`):

```bash
/opt/php8.4/bin/php artisan tinker --execute="
  App\Models\File::whereKey(8)->update(['label' => 'v2']);
  App\Models\File::whereKey(10)->update(['label' => 'v3']);"
```

(то же самое из админки — `PATCH admin/files/{file}` с токеном Sanctum.)

---

## 5. Issues

| Issue | Фаза | Содержание | Статус |
|---|---|---|---|
| #35 | 0 | Выгрузить хвост file 8 (модерация 368 отброшена) | открыт, без срока |
| #36 | 1 | `FILE_ID = 8` → конфиг в 5 файлах отчётов | ✅ закрыт |
| #37 | 1 | Фильтр активного датасета в `DailyQuotaController`, `ProfileReportController`, `Verify/UserController` | ✅ закрыт |
| #38 | 1 | Фильтр файла в удерживаемом тексте `SpeakTextController` + очередях модерации | ✅ закрыт |
| #39 | 1 | Конфигурируемый `--file-id` в командах экспорта | ✅ закрыт |
| #40 | 2 | Импорт нового датасета (+ чанковый `insert`, проверка дублей, `--file-id` в `import:excel-file`) | ✅ закрыт |
| #41 | 3 | Переключение прода и постпроверка | ✅ закрыт (остались 223 удерживаемых текста) |
| #42 | 4 | `files.label`, `is_active` аксессором, отображение в админке, имена групп экспорта | ✅ закрыт, на проде |
| #44 | — | Медленные админские списки: привязка `finished/*` к активному датасету, индексы под сортировку, `verify/users` без сортировки по алиасу | код готов, ждёт выката |

---

## Приложение: команды проверки на проде

```bash
cd /var/www/fastuser/data/www/api.verify.aralhub.uz

# Тексты по датасетам
/opt/php8.4/bin/php artisan tinker --execute="
  print_r(DB::table('texts')->selectRaw('file_id, is_main, is_split_part, count(*) as c')
    ->groupBy('file_id','is_main','is_split_part')->orderBy('file_id')->get()->toArray());"

# Аудио по датасетам: озвучено / без модерации / принято / выгружено
/opt/php8.4/bin/php artisan tinker --execute="
  print_r(DB::table('audio')->join('texts','audio.text_id','=','texts.id')
    ->selectRaw('texts.file_id, count(*) as total,
       sum(audio.speak_finished_at is not null) as spoken,
       sum(audio.is_correct is null and audio.speak_finished_at is not null) as unmoderated,
       sum(audio.is_correct=1) as correct,
       sum(audio.exported_at is not null) as exported')
    ->groupBy('texts.file_id')->get()->toArray());"
```

Деплой — `./.deploy.sh` из каталога приложения (см. `README.md` §Deployment).
На сервере системный `php` — 8.2.24, актуальный интерпретатор — `/opt/php8.4/bin/php`.
